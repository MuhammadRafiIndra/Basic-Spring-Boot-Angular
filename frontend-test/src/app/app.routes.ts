import { Routes } from '@angular/router';
import { MyComponent } from './features/my-component/my-component';

export const routes: Routes = [
    {
        path: "my-component",
        component: MyComponent
    }
];
